{
  "name": "profile",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "stamp-version": "bash scripts/stamp-version.sh",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.80.0"
  }
}
